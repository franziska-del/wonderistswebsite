{
  "name": "the-wonderists",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "pnpm build && wrangler pages deploy"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "wrangler": "^4.128.0"
  }
}
